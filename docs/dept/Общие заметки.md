## Общие заметки


1) Убрать вывод version в Ticket
2) Проверить, что происходит с заявками, если клиент отключается
3) Проверить что просходит с заявками, если пользватель клиента отключается
4) Проверить, что просходит с заявками, если контактное лицо пользователя клиента отключается
5) Проверить, что просходит с заявками пользователя, если пользователь отключается
6) Проверить, что просходит с заявками пользователя, если отключается контактное лицо
7) Проверить, что происходит с заявками пользователями если клиент отключается
8) ~~Проверить, что происходит с пользователями, если клиент отключается~~
9) Если указан user_id и не указан contact_user_id, то contact_user_id=user_id
10) Не использовать comment-ы в запросе для тех статусов, где они не нужны.
11) При обновлении данных клиента, если не указано имя выдавать ошибку, а не оставлять прежнее имя
12) В /admin/tickets/{ticket_id}/schedule можно указать планируемую дату раньше текущей. Нужно ли это?
13) Если отключается department, что делать с сотрудниками и заяаками где это department участвует?
14) Если клиент переходит в disable, то заявки переходят в отложенное с заранее определенным комментарием. возможно все равно устанавливать комментарий оператором.
15) Можно ли отключать Admin, если он установлен в заявках? Скорее да, просто эти заявки должны переводиться в другое сотосния без исполнителя. Отключение Admin-а это такая операция, которая может быть внезапной.

