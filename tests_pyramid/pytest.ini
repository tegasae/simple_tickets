[pytest]
addopts = -q --strict-markers
markers =
    unit: pure unit test
    integration: integration with SQLite/FastAPI boundary
    contract: adapter/schema contract test
    web: FastAPI/web-layer test
